// models/index.js
const Comment = require('./Comment');
const User = require('./User');
const Post = require('./Post');

// Define any associations or relationships between models here, if needed

module.exports = { Comment, User, Post };
